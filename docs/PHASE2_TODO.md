# Phase 2 Todo List

本文件用于跟踪二期迭代开发顺序、可验收标准和完成状态。每完成一个模块，更新对应勾选状态。

## 开发顺序

- [x] Phase 1：数据导入体验升级 + 示例数据一键导入
  - [x] CSV 拖入后先本地预览，不自动上传 OSS
  - [x] 支持字段映射：`product_id`、`review_score`、`review_content` 等
  - [x] 导入前展示预计导入行数和前 20 行预览
  - [x] 支持按文件 hash 检测重复导入风险
  - [x] 点击确认后才真正上传并创建导入任务
  - [x] 支持内置小型 Olist 示例数据一键导入
- [ ] Phase 2：自定义标签库 + 问题解决方案库
- [ ] Phase 3：Prompt 模板管理 + AI 调用日志
- [ ] Phase 4：类目聚合分析 + 报告归档
- [ ] Phase 5：PDF 导出 + Docker 一键部署

## 二期验收标准

- [ ] 新用户 clone 项目后，按 README 能在 10 分钟内跑起来。
- [x] 可以导入示例数据。
- [ ] 可以一键生成评论分析和 AI 报告。
- [ ] 用户能维护自己的标签和 Prompt。
- [ ] 系统能展示 AI 调用次数和成本估算。
- [ ] 能导出一份像样的 PDF 运营报告。
- [ ] GitHub README 看起来像成熟项目，而不是临时作业。

## Phase 1 验收方式

- 上传单个 CSV 后，页面显示字段映射、预计行数和前 20 行预览。
- 拖入 CSV 时不会立刻上传 OSS，点击“开始导入”后才创建导入任务。
- 重复选择同一个 CSV 时，页面能提示疑似重复导入。
- 点击“一键导入示例数据”后，系统创建 CSV 导入任务并写入内置样例评论。
